<%#=checks to make sure someone is logged in %>
<% if logged_in? %>

<%#=if logged in & an admin %>
<% if is_admin? %>

<h3>All Users</h3>
<ul class="users">
  <%= render @users %>
</ul>

<%#=if logged in as user & startdate not set: %>
<%elsif !start_date_set? %>
<h2>Enter the start date of your internship and daily logs will be automatically generated each day at midnight</h2>
<%= render 'shared/start_date_form' %>

<%#=if logged in & user & startdate set: %>
<%else %>
  <div class="row">
        <div class="col-md-12">
      <h2>Daily Logs</h2>
      <%= render 'shared/feed' %>
    </div>
  </div>

 <% end %> 
  

 <%#=if not logged in %> 
<% else %>
<div class="center jumbotron">
  <h1>Welcome to the Online Journal</h1>

  <h2>
    This is the home page for the
    Online Journal
  </h2>

  <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>

  <h2>Already signed up?</h2>

  <%= link_to "Log in", login_path, class: "btn btn-md btn-primary" %>
</div>

<% end %>